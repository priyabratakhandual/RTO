apiVersion: v1
kind: Service

metadata:
  name: student-registration-service

spec:
  type: LoadBalancer

  selector:
    app: student-registration

  ports:
  - protocol: TCP
    port: 80
    targetPort: 5008